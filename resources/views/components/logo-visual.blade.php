<div>
    <!<a href="{{ url('/') }}">
        <img 
            src="{{ asset('images/LOGO_IMG_TRANSPARENTE.png') }}" 
            alt="Logo"
            class="hover:opacity-80 transition {{ $class }}"
        />
    </a>
</div>